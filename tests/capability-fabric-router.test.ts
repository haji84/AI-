import assert from "node:assert/strict";import test from "node:test";import{routeFabricJob,type FabricNode}from "../src/orchestrator/capability-fabric-router.ts";const nodes:FabricNode[]=[{id:"local",kind:"local",capabilities:["xlsx","reason"],online:true,healthy:true,privacy:"local-only",incrementalCost:0,latencyMs:30,reliability:.9,evidenceModes:["LOCAL"]},{id:"online",kind:"online",capabilities:["reason","research"],online:true,healthy:true,privacy:"public-ok",incrementalCost:0,latencyMs:100,reliability:.98,evidenceModes:["ONLINE"]},{id:"gpu",kind:"gpu",capabilities:["reason","image"],online:true,healthy:true,privacy:"redacted-ok",incrementalCost:0,latencyMs:40,reliability:.95,evidenceModes:["LOCAL"]}];
test("fabric routes secret work local and current online research online",()=>{assert.equal(routeFabricJob({id:"a",requiredCapabilities:["reason"],privacy:"secret",freshness:"static",risk:"low"},nodes).node?.id,"local");assert.equal(routeFabricJob({id:"b",requiredCapabilities:["research"],privacy:"public",freshness:"current",risk:"low",evidenceMode:"ONLINE"},nodes).node?.id,"online")});
test("fabric fails closed rather than leaking secret or silently spending",()=>{const r=routeFabricJob({id:"x",requiredCapabilities:["research"],privacy:"secret",freshness:"current",risk:"high",evidenceMode:"ONLINE"},nodes);assert.equal(r.blocked,true);assert.equal(r.node,null)});
