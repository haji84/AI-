import assert from "node:assert/strict";import test from "node:test";import{queueDeferredVerification,reconnectDeferred,resolveDeferred}from "../src/orchestrator/deferred-verification.ts";
test("offline current fact waits, reconnects, and requests regeneration on changed verified fact",()=>{const q=queueDeferredVerification({id:"d",goalId:"g",claimIds:["current"],artifactRefs:["report.xlsx"],createdAt:"2026-09-22T00:00:00Z"});assert.equal(q.status,"WAITING_CONNECTIVITY");assert.equal(reconnectDeferred(q,false).status,"WAITING_CONNECTIVITY");const ready=reconnectDeferred(q,true);assert.equal(ready.status,"READY");assert.equal(resolveDeferred(ready,{factVerified:true,artifactStillMatches:false}).status,"REGENERATION_REQUIRED")});
test("reconnect never converts unverified current fact into success",()=>{const q=reconnectDeferred(queueDeferredVerification({id:"d",goalId:"g",claimIds:["c"],artifactRefs:[],createdAt:"x"}),true);assert.equal(resolveDeferred(q,{factVerified:false,artifactStillMatches:true}).status,"BLOCKED")});
