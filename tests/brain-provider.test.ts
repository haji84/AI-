import assert from "node:assert/strict";import test from "node:test";import{BrainProviderRegistry,executeBrain,type BrainProvider}from "../src/orchestrator/brain-provider.ts";const p:BrainProvider={id:"future-brain",class:"frontier",async available(){return true},async execute(){return{ok:true,output:"42",evidenceRefs:["e"],providerId:this.id,modelId:"x",cost:0}}};test("brain contract is provider/model independent and evidence/cost governed",async()=>{const reg=new BrainProviderRegistry().register(p);assert.equal(reg.get("future-brain")?.class,"frontier");assert.equal((await executeBrain(p,{task:"t",context:[],maxCost:0,requiresEvidence:true})).ok,true);const noEvidence={...p,id:"bad",async execute(){return{ok:true,output:"42",evidenceRefs:[],providerId:"bad",cost:0}}};assert.equal((await executeBrain(noEvidence,{task:"t",context:[],maxCost:0,requiresEvidence:true})).ok,false)});