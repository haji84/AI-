import assert from "node:assert/strict";import test from "node:test";import{evaluateRiskPolicy}from "../src/orchestrator/risk-policy.ts";test("safe autonomy auto-runs low and verified medium but never bypasses high/critical gates",()=>{assert.equal(evaluateRiskPolicy({}).autoExecutionAllowed,true);const medium=evaluateRiskPolicy({crossComponentChange:true},{ciPassed:true,qaPassed:true,reviewerPassed:true,unresolvedReviewThreads:0,destructiveChangeAbsent:true,privilegedChangeAbsent:true});assert.equal(medium.autoExecutionAllowed,true);assert.equal(medium.humanApprovalRequired,false);const high=evaluateRiskPolicy({billingOrContractChange:true});assert.equal(high.autoExecutionAllowed,false);assert.equal(high.humanApprovalRequired,true);const critical=evaluateRiskPolicy({protectionOrAuditDisable:true});assert.equal(critical.executionBlocked,true);assert.equal(critical.autoExecutionAllowed,false)});