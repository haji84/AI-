import assert from "node:assert/strict";import test from "node:test";import{DeviceSandboxCapability,verifyDevice,type DeviceSandboxState}from "../src/orchestrator/device-sandbox-capability.ts";import type{WorkAction,WorkVerifierContract}from "../src/orchestrator/work-capability.ts";
const v:WorkVerifierContract={kind:"device.observed",required:true,spec:{screen:"on"}};function a(strategyId:string,operation:string):WorkAction{return{goalId:"g",jobId:"j",attemptId:`a-${strategyId}`,strategyId,capability:"device.act",domain:"device",operation,input:{},scope:[{kind:"device",ids:["fixture"]}],expectedOutputs:["observed screen on"],risk:"low",access:"act",externalSideEffect:false,irreversible:false,verifier:v}}
test("device verifier requires observed state and recovers after retry",async()=>{const s:DeviceSandboxState={online:false,screen:"off"};const c=new DeviceSandboxCapability(s);const first=await c.execute(a("initial","wake"));assert.equal(first.ok,false);assert.equal(verifyDevice(s,v).ok,false);s.online=true;const retry=await c.execute(a("recovery","wake"));assert.equal(retry.ok,true);assert.equal(verifyDevice(s,v).ok,true);});
