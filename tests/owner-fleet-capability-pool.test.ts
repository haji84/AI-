import assert from "node:assert/strict";import test from "node:test";import{selectOwnerFleetNodes,assertOwnerFleetTarget}from "../src/orchestrator/owner-fleet-capability-pool.ts";import type{JarvisNode}from "../src/jarvis/types.ts";const node=(id:string,kind:JarvisNode["kind"],caps:JarvisNode["capabilities"]):JarvisNode=>({id,label:id,kind,status:"ready",capabilities:caps,policy:{allowPaidServices:false,allowDestructiveActions:false,allowExternalPublication:false,allowRemoteControl:true,requireHumanForLockedDevice:true},telemetry:{checkedAt:"now"},enrollment:"full",lastSeenAt:"now"});const nodes=[node("zbook","windows",["windows-tooling","filesystem","gpu"]),node("macbook","macos",["filesystem","browser"]),node("iphone","ios",["camera","gps"]),node("stranger","windows",["windows-tooling"])];const members=[{nodeId:"zbook",ownerId:"owner",verifiedIdentity:true,shareCapabilities:true},{nodeId:"macbook",ownerId:"owner",verifiedIdentity:true,shareCapabilities:true},{nodeId:"iphone",ownerId:"owner",verifiedIdentity:true,shareCapabilities:true},{nodeId:"stranger",ownerId:"other",verifiedIdentity:true,shareCapabilities:true}];test("verified owner devices share capabilities while foreign device is excluded",()=>{assert.deepEqual(selectOwnerFleetNodes(nodes,members,{ownerId:"owner",requiredCapabilities:["windows-tooling"]}).map(n=>n.id),["zbook"]);assert.deepEqual(selectOwnerFleetNodes(nodes,members,{ownerId:"owner",requiredCapabilities:["camera"]}).map(n=>n.id),["iphone"]);assert.deepEqual(selectOwnerFleetNodes(nodes,members,{ownerId:"owner",requiredCapabilities:["filesystem"]}).map(n=>n.id),["macbook","zbook"]);assert.throws(()=>assertOwnerFleetTarget("stranger",members,"owner"),/not an authorized/)})