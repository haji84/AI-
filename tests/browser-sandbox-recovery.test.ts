import assert from "node:assert/strict";import test from "node:test";import {BrowserSandboxCapability,verifyBrowser,type BrowserSandboxState} from "../src/orchestrator/browser-sandbox-capability.ts";import type{WorkAction,WorkVerifierContract}from "../src/orchestrator/work-capability.ts";
const v:WorkVerifierContract={kind:"browser.state",required:true,spec:{url:"https://fixture.local/form",fields:{name:"JARVIS"},submitted:true}};
function a(strategyId:string,operation:string,input:Record<string,unknown>):WorkAction{return{goalId:"g",jobId:"j",attemptId:`a-${strategyId}`,strategyId,capability:"browser.act",domain:"browser",operation,input,scope:[{kind:"origin",ids:["https://fixture.local"]}],expectedOutputs:["verified state"],risk:"low",access:"act",externalSideEffect:false,irreversible:false,verifier:v}}
test("browser sandbox FAIL -> evidence -> recovery -> PASS",async()=>{const s:BrowserSandboxState={url:"",fields:{},submitted:false};const c=new BrowserSandboxCapability(s);await c.execute(a("initial","navigate",{url:"https://fixture.local/wrong"}));await c.execute(a("initial","fill",{field:"name",value:"WRONG"}));let x=verifyBrowser(s,v);assert.equal(x.ok,false);assert.ok(x.evidence.mismatches.length>=2);await c.execute(a("recovery","navigate",{url:"https://fixture.local/form"}));await c.execute(a("recovery","fill",{field:"name",value:"JARVIS"}));await c.execute(a("recovery","submit",{}));x=verifyBrowser(s,v);assert.equal(x.ok,true);});
