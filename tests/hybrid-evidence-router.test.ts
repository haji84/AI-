import assert from "node:assert/strict";import test from "node:test";import{routeEvidence}from "../src/orchestrator/hybrid-evidence-router.ts";const c=[{id:"local-old",channel:"LOCAL" as const,available:true,authoritative:true,fresh:false,privacySafe:true,cost:0},{id:"official-web",channel:"ONLINE" as const,available:true,authoritative:true,fresh:true,privacySafe:true,cost:0},{id:"device",channel:"DEVICE" as const,available:true,authoritative:false,fresh:true,privacySafe:true,cost:0}];test("current authoritative fact routes to fresh authoritative evidence, static local fact can stay local",()=>{assert.equal(routeEvidence({requiresCurrent:true,requiresAuthoritative:true,maxCost:0},c).source?.id,"official-web");assert.equal(routeEvidence({channels:["LOCAL"],requiresCurrent:false,requiresAuthoritative:true,maxCost:0},c).source?.id,"local-old")});test("router fails closed when privacy or freshness policy cannot be satisfied",()=>{assert.equal(routeEvidence({channels:["ONLINE"],requiresCurrent:true,requiresAuthoritative:true,maxCost:0},c.map(x=>({...x,privacySafe:false}))).blocked,true)});