import assert from "node:assert/strict";import test from "node:test";import{MultiWorkerRuntime,createFunctionWorker}from "../src/gai/worker-runtime.ts";import{WindowsRealMachineVerifier}from "../src/orchestrator/windows-real-machine-verifier.ts";const task={id:"real",description:"ZBook real-machine smoke",difficulty:1,risk:"LOW" as const,requiredCapabilities:["windows-tooling"],requiresFrontierReasoning:false};
test("real-machine verifier selects healthy Windows tooling worker and hashes observed output",async()=>{const worker=createFunctionWorker({descriptor:{id:"zbook",label:"ZBook",platform:"windows",deviceType:"laptop",capabilities:["windows-tooling"],maxParallelTasks:1,enabled:true,executionModes:["resident"],verifierHooks:{executionEvidence:true}},run:async()=>"{\"machine\":\"zbook\",\"pass\":true}"});const r=await new WindowsRealMachineVerifier(new MultiWorkerRuntime([worker])).verify({task,operation:"smoke",preferredWorkerId:"zbook"});assert.equal(r.evidence.workerId,"zbook");assert.equal(r.evidence.platform,"windows");assert.match(r.evidence.outputSha256,/^[a-f0-9]{64}$/)});