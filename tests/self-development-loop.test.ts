import assert from "node:assert/strict";import test from "node:test";import{createDevelopmentState,nextDevelopmentState}from "../src/orchestrator/self-development-loop.ts";test("self development requires inspect research implement test and independent verification evidence",()=>{let s=createDevelopmentState();for(const stage of ["INSPECT","RESEARCH","IMPLEMENT","TEST","INDEPENDENT_VERIFY"] as const)s=nextDevelopmentState(s,{stage,ok:true,evidenceRefs:["e:"+stage]});assert.equal(s.stage,"DONE")});test("failed test recovers but evidence-free success can never advance",()=>{let s=createDevelopmentState();s=nextDevelopmentState(s,{stage:"INSPECT",ok:true,evidenceRefs:["e"]});s=nextDevelopmentState(s,{stage:"RESEARCH",ok:true,evidenceRefs:["e"]});s=nextDevelopmentState(s,{stage:"IMPLEMENT",ok:true,evidenceRefs:["e"]});s=nextDevelopmentState(s,{stage:"TEST",ok:false,evidenceRefs:["fail"],reason:"test failed"});assert.equal(s.stage,"RECOVER");s=nextDevelopmentState(s,{stage:"RECOVER",ok:true,evidenceRefs:["fix"]});assert.equal(s.stage,"IMPLEMENT");assert.equal(nextDevelopmentState(s,{stage:"IMPLEMENT",ok:true,evidenceRefs:[]}).stage,"BLOCKED")});