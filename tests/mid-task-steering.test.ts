import assert from "node:assert/strict";import test from "node:test";import{applyGoalDelta}from "../src/orchestrator/mid-task-steering.ts";import type{WorkState}from "../src/orchestrator/work-state.ts";
test("mid-task steering preserves verified work and adds only new DoD",()=>{const state:WorkState={goalId:"g",objective:"report",definitionOfDone:[{id:"xlsx",description:"xlsx"}],currentState:"xlsx done",status:"COMPLETED",riskClass:"R1",constraints:[],decisions:[],artifacts:[{id:"a",uri:"file:x.xlsx"}],verificationResults:[{itemId:"xlsx",passed:true,evidence:"hash"}],childWorkItems:[],blockers:[],nextAction:null,updatedAt:"old"};const r=applyGoalDelta({title:"report",successCriteria:["xlsx"],constraints:[]},state,{descriptionAppend:"PDFも追加",addSuccessCriteria:["pdf"],addDefinitionOfDone:[{id:"pdf",description:"pdf"}]},"new");assert.equal(r.workState.status,"IN_PROGRESS");assert.deepEqual(r.preservedVerificationIds,["xlsx"]);assert.equal(r.workState.artifacts.length,1);assert.deepEqual(r.addedDefinitionOfDoneIds,["pdf"]);assert.equal(r.workState.nextAction,"replan_goal_delta")});
