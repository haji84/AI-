import assert from "node:assert/strict";import test from "node:test";import{ProductionBrowserCapability,type ProductionBrowserDriver}from "../src/orchestrator/production-browser-capability.ts";test("production browser records observed page evidence and rejects origin escape",async()=>{let current="";const driver:ProductionBrowserDriver={async navigate(url){current=url;return{url,text:"",capturedAt:"t"}},async read(){return{url:current,title:"Official",text:"amount=42",capturedAt:"2026-09-22T00:00:00Z"}}};const r=await new ProductionBrowserCapability(driver).navigateAndRead("https://example.go.jp/facts");assert.equal(r.observation.text,"amount=42");assert.match(r.evidence.sha256,/^[a-f0-9]{64}$/);const bad:ProductionBrowserDriver={async navigate(url){return{url,text:"",capturedAt:"t"}},async read(){return{url:"https://evil.example/x",text:"x",capturedAt:"t"}}};await assert.rejects(()=>new ProductionBrowserCapability(bad).navigateAndRead("https://example.go.jp"),/unexpected origin/)})