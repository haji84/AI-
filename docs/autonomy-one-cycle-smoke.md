# One-Cycle Bounded Command Smoke

This marker verifies the production Mobile Autonomy path after the explicit-command completion fix.

Expected behavior: one explicit bounded plan executes once, verification passes, Compass/write-back records success, and the run terminates with `goal_complete` without replay.

Merge remains an explicit Human Gate.
