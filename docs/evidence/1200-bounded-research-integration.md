# #1200 bounded research integration evidence

Code revision: febc171aed0be8f82e3bba3902d91d6b66d3a59c. Baseline: f974cc39dc25697e6cf999764e9d695ee753e14d. Date:2026-09-23 JST. Parent #681 / master #882.

## Delivered scope

The existing explicit unified plan can request structured public JSON fact checks through autonomy.delegate. Exact host-approved URL/field policy, pinned HTTPS with all #1187 resource/network controls, immutable action snapshot, receipt-bound independent deterministic verification, citations/hash metadata and durable success/failure evidence are connected to scripts/autonomy-cloud-run.ts. No alternative Goal authority or owner frontend is introduced.

## Verification

- pnpm test:1416 PASS,0 fail/skip.
- pnpm test:p8-security:296 PASS,0 fail/skip, now includes research transport and runtime integration negative tests.
- pnpm lint and pnpm build:PASS. Existing video-plan-store dynamic tracing warning remains.
- Built app localhost3194 health:HTTP200/status ok. Missing-auth/config operation routes reject.
- Independent review reproduced and then verified repairs for in-flight input rebinding and malformed receipt serialization; failed fact evidence now persists in Work-State as well as Compass.
- Original integration tests failed before implementation. JSON Accept regression failed before transport correction. A generated-test regex escaping error was corrected before final tests; an initial build ran before dependency installation completed, then passed after installation. No tests were weakened.

## Actual existing-script execution on Windows

Two separate node processes ran scripts/autonomy-cloud-run.ts --mode=run --max-cycles=1 with isolated temporary Compass DBs and explicit temporary host policies. No credentials, model API, Broker or production runtime was used. Public npm JSON at https://registry.npmjs.org/-/package/typescript/dist-tags returned a supported claim and citations; an intentionally incorrect expected value returned CONFLICTED/BLOCKED. This proves bounded software/network integration, not PHYSICAL device evidence, universal fact accuracy, or product completion. The script's goal_complete identifies its bounded command; the product DoD is not completed.

Earlier live endpoint /typescript/latest returned JSON-looking text/plain and was correctly denied; GitHub public API rejected the native request. Neither MIME nor HTTP restrictions were relaxed. Explicit JSON Accept was added and separately regression-tested. Live artifacts contain body hashes and timestamps; the public latest tag may change, so the fixed expected value is a recorded test condition, not a permanent truth. Reproducer:node docs/evidence/1200/verify-live-network.mjs from this checkout.

## Safety / rollback / remaining work

No production policy, credential, permission, device version, enrollment, queue, schema, firewall or network exposure changed. New acquisition defaults to deny until a host policy is legitimately configured. Return to the earlier context-only path by reverting this additive implementation; retain historical evidence.

Full CORE-012 and GOV-008 remain PARTIAL. Arbitrary prose/PDF/OCR extraction, semantic entailment, broad independent-source analysis, organization claim authority and owner report UI remain incomplete. #1195 correction/Skill runtime is independently CI-green but unmerged pending actual-device acceptance. Native Windows worker execution and physical/recovery acceptance remain separate work. All340 requirement IDs/statuses/evidence obligations are retained.

## Current main compatibility

Main ab792ecaac73c961a12141d437971134102f6515 adds UI-007 reconciliation only. Preserved and merged at ee294bd5fff4278f269f6a82e237e12bc5aaffa5; full tests1418/1418 PASS and340-row ledger PASS. PR #1206 contains the scoped research integration. PR #1195 remains held for physical acceptance.

CI run35770434323 failed lint on the added standalone evidence reproducer because the docs directory has no implicit Node globals. Added explicit node:process and node:console imports; no lint/test rule was disabled. Exact-head CI must pass on the correction.
