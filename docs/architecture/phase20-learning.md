# Phase 20 learning boundary

Learning hooks fire only when the underlying cycle contains a passing verifier result. This prevents dispatch success or unverified output from being treated as reusable knowledge. Existing memory, skill, organizational-memory and continual-learning components decide what verified evidence is eligible for promotion.
