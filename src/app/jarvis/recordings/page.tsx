import RecordingHistory from "./RecordingHistory.tsx";

export const dynamic = "force-dynamic";

export default function JarvisRecordingHistoryPage() {
  return (
    <main className="dashboard-shell">
      <RecordingHistory />
    </main>
  );
}
