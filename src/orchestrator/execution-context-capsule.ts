export interface ExecutionContextCapsule{goal:string;currentJob:string;why:string;workflowPosition:string;inputs:string[];constraints:string[];decisions:string[];dependencies:string[];expectedOutput:string[];definitionOfDone:string[];verificationContract:string;recoveryContext:string[];}
export interface CapsuleValidation{ok:boolean;missing:string[];}
export function verifyContextCapsule(c:ExecutionContextCapsule):CapsuleValidation{const required:[keyof ExecutionContextCapsule,unknown][]=[["goal",c.goal],["currentJob",c.currentJob],["why",c.why],["workflowPosition",c.workflowPosition],["inputs",c.inputs],["constraints",c.constraints],["decisions",c.decisions],["dependencies",c.dependencies],["expectedOutput",c.expectedOutput],["definitionOfDone",c.definitionOfDone],["verificationContract",c.verificationContract],["recoveryContext",c.recoveryContext]];const missing=required.filter(([,v])=>typeof v==="string"?!v.trim():!Array.isArray(v)).map(([k])=>String(k));return{ok:missing.length===0,missing}}
export type ContextRequestDecision={status:"APPROVED"|"LOCAL_RESOLUTION_REQUIRED"|"EXTERNAL_DISCLOSURE_REQUIRED";payload?:string;reason:string};
export function resolveProgressiveContextRequest(input:{requested:string;sensitivity:"public"|"internal"|"personal"|"secret";canGeneralize:boolean;localResolutionAvailable:boolean}):ContextRequestDecision{if(input.sensitivity==="public")return{status:"APPROVED",payload:input.requested,reason:"public context"};if(input.canGeneralize)return{status:"APPROVED",payload:"[GENERALIZED_CONTEXT_REQUIRED]",reason:"sensitive context must be generalized locally before release"};if(input.localResolutionAvailable)return{status:"LOCAL_RESOLUTION_REQUIRED",reason:"resolve sensitive dependency locally and return only the result"};return{status:"EXTERNAL_DISCLOSURE_REQUIRED",reason:"specific sensitive context would need external disclosure"}}
