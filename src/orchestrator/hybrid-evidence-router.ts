export type EvidenceChannel="LOCAL"|"ONLINE"|"CONNECTED"|"DEVICE";
export interface EvidenceSourceCandidate{id:string;channel:EvidenceChannel;available:boolean;authoritative:boolean;fresh:boolean;privacySafe:boolean;cost:number;}
export interface EvidenceNeed{channels?:EvidenceChannel[];requiresCurrent:boolean;requiresAuthoritative:boolean;maxCost:number;}
export function routeEvidence(need:EvidenceNeed,candidates:EvidenceSourceCandidate[]){const eligible=candidates.filter(c=>c.available&&c.privacySafe&&c.cost<=need.maxCost&&(!need.channels||need.channels.includes(c.channel))&&(!need.requiresCurrent||c.fresh)&&(!need.requiresAuthoritative||c.authoritative));const ranked=[...eligible].sort((a,b)=>Number(b.authoritative)-Number(a.authoritative)||Number(b.fresh)-Number(a.fresh)||a.cost-b.cost||a.id.localeCompare(b.id));return{source:ranked[0]??null,alternatives:ranked.slice(1),blocked:ranked.length===0,reason:ranked.length?"evidence source selected":"no policy-compliant evidence source"}}
